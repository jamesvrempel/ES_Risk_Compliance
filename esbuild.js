/**
 * esbuild configuration for ES Risk and Compliance Management
 * This module has no frontend assets to build
 */

// Empty build script - no assets to compile
console.log('ES Risk and Compliance Management: No frontend assets to build');
process.exit(0);
